# Core TTS model (pulls torch, numpy, transformers, etc. as deps)
chatterbox-tts>=0.1.7

# Server
fastapi
uvicorn

# Audio post-processing (not included in chatterbox-tts)
# NOTE: auto-editor installed separately due to dep conflicts (see Dockerfile)
pyrnnoise>=0.3.8
pyloudnorm>=0.2.0
pydub
ffmpeg-python

# Speech validation
openai-whisper
faster-whisper

# Text processing
nltk

# Gradio UI (optional, for standalone use)
gradio
spaces
