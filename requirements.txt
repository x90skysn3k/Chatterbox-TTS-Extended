# Core TTS model (pulls torch, numpy, transformers, etc. as deps)
chatterbox-tts>=0.1.7

# Server
fastapi
uvicorn

# Audio post-processing (not included in chatterbox-tts)
# auto-editor pinned to older version compatible with numpy<2
auto-editor>=25.0.0,<27
pyrnnoise>=0.3.8
pyloudnorm>=0.2.0
pydub
ffmpeg-python

# Speech validation
openai-whisper
faster-whisper

# Text processing
nltk

# Gradio UI (optional, for standalone use)
gradio
spaces
