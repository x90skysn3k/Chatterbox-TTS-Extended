# Chatterbox Extended TTS Server

## Commit Rules
- Do NOT add `Co-Authored-By` lines to commits — commit as the git user only
- Do NOT amend commits from other branches or agents

## Project Notes
- Server runs on P40 GPU (CUDA) at your-server:8004
- Fallback: M1 Max (MPS) locally
- Another agent may be working on fixes in the main branch — keep worktree changes isolated
